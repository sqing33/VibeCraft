## 1. Shared shell foundation

- [x] 1.1 Create a shared workspace shell component and centralize workspace-route rendering in `ui/src/App.tsx`
- [x] 1.2 Move the fixed top navigation and bottom status/utility controls into the shared shell

## 2. Lane integration

- [x] 2.1 Refactor Chat to provide session-sidebar content and conversation-panel content inside the shared shell
- [x] 2.2 Refactor Repo Library list, search, and detail routes to reuse the shared shell and persistent repository sidebar
- [x] 2.3 Refactor Orchestrations list and detail routes to reuse the shared shell and persistent recent-orchestrations sidebar

## 3. Verification and docs

- [x] 3.1 Update `PROJECT_STRUCTURE.md` for the new shared shell responsibility
- [x] 3.2 Run the UI build and verify the shared-shell route mapping
