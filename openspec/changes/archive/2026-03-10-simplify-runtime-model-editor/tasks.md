## 1. Spec And Compatibility

- [x] 1.1 Update OpenSpec proposal, design, and delta specs for the simplified runtime model editor
- [x] 1.2 Make runtime model save normalization derive omitted provider/model fields from source and model id

## 2. Runtime Model UI

- [x] 2.1 Simplify the runtime model editor fields to model, label, and API source
- [x] 2.2 Replace the runtime-level default selector with per-card default actions in a responsive grid
- [x] 2.3 Keep model test and delete actions working from the new card layout

## 3. Validation

- [x] 3.1 Add or update focused tests for runtime model normalization behavior
- [x] 3.2 Run frontend build and backend tests for the refactor
