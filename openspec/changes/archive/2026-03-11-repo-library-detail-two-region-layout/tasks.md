## 1. Layout Reframe

- [x] 1.1 Replace the current three-region workspace with a `2:3` left-right split
- [x] 1.2 Move snapshot and analysis selectors into the top panel of the left region
- [x] 1.3 Move repository context into the bottom panel of the left region with independent scrolling

## 2. Card Navigation Rework

- [x] 2.1 Convert the card list into a horizontal selection strip at the top of the right region
- [x] 2.2 Reduce each card item to title plus type tag, with a compact selected state
- [x] 2.3 Keep the card detail pane as the dominant reading surface below the selection strip

## 3. Verification & Spec Sync

- [x] 3.1 Validate the updated repository detail page in the browser
- [x] 3.2 Sync the workspace spec to match the new two-region layout after implementation
