[features]
multi_agent = true

[agents]
max_threads = 8
max_depth = 1

[agents.explorer]
description = "Read-only repository explorer focused on evidence-backed implementation analysis."
config_file = "agents/explorer.toml"
