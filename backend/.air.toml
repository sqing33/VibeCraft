root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/vibe-tree-daemon ./cmd/vibe-tree-daemon"
  entrypoint = ["./tmp/vibe-tree-daemon"]
  include_ext = ["go", "mod", "sum"]
  exclude_dir = ["tmp", "vendor"]
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
